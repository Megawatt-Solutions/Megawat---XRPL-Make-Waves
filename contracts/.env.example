# Stream Vault demo — Base Sepolia
PRIVATE_KEY=
BASESCAN_API_KEY=
RPC_URL=https://sepolia.base.org

# Optional: separate signer wallets for the 2-of-3 demo (default: deployer holds all three roles)
# AGENT_ADDRESS=
# ENGINEER_ADDRESS=

# Optional: demo clock, seconds per "month" (default 600)
# SECONDS_PER_MONTH=600
